import React from 'react'
import {table} from '../../../types/index'

interface IProps {
    data: table[];
}

export default function Index(props: IProps) {
    return (
        <div>
            <p>Hello</p>
        </div>
    )
}
